{
  "name": "webdumdum",
  "version": "1.0.0",
  "private": true,
  "description": "Web DUM DUM — HTML generado con Node + esbuild (islas de JS, carta pintada en el edge)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
